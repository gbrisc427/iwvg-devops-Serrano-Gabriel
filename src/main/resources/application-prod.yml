server:
  port: 10000

spring:
  datasource:
    url: jdbc:h2:file:/app/data/devops;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
